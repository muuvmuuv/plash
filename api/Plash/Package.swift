// swift-tools-version: 5.7

import PackageDescription

let package = Package(
  name: "Plash",
  dependencies: [
    // .package(url: /* package url */, from: "1.0.0"),
  ],
  targets: [
    .target(
      name: "Plash",
      dependencies: [])
  ]
)
